NAME="llm-tool-gate"
TYPE="review"
HOOKS="pre_tool"
PRIORITY=50
FAIL="closed"
ENABLED=0
DESC="Ask the LLM bridge for an allow/deny verdict on a proposed tool call. Off by default (costs an extra model call)."
